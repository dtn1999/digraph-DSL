<<<<<<< HEAD
/* (c) https://github.com/MontiCore/monticore */
pluginManagement {
  repositories {
    if(("true").equals(getProperty('useLocalRepo'))){
      mavenLocal()
    }
    maven {
      credentials.username mavenUser
      credentials.password mavenPassword
      url repo
    }
    gradlePluginPortal()
  }
}
rootProject.name = 'automaton'
=======
/* (c) https://github.com/MontiCore/monticore */
pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        maven {
            credentials.username mavenUser
            credentials.password System.getenv("cibuild")
            url repo
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'GraphQL Lite'
>>>>>>> a6445e8 (first commit)
